events{}
http {
    upstream lucky_prize-swarm {
        server manager;
        server worker-1;
    }
    server {
        location / {
            proxy_pass http://lucky_prize-swarm;
        }
    }
}